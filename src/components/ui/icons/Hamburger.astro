<button
  id="menu-button"
  class="ml-auto cursor-pointer transition-transform duration-200 hover:scale-110 md:hidden"
>
  <svg
    width="24"
    height="24"
    viewBox="0 0 24 24"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
    class="hamburger-icon"
  >
    <path
      id="menu-button-line-1"
      class="hamburger-line hamburger-line-top"
      d="M3 6H21"
      stroke="currentColor"
      stroke-width="2.5"
      stroke-linecap="round"
      stroke-linejoin="round"></path>
    <path
      id="menu-button-line-2"
      class="hamburger-line hamburger-line-middle"
      d="M3 12H21"
      stroke="currentColor"
      stroke-width="2.5"
      stroke-linecap="round"
      stroke-linejoin="round"></path>
    <path
      id="menu-button-line-3"
      class="hamburger-line hamburger-line-bottom"
      d="M3 18H21"
      stroke="currentColor"
      stroke-width="2.5"
      stroke-linecap="round"
      stroke-linejoin="round"></path>
  </svg>
</button>

<style>
  .hamburger-line {
    transition: all 0.3s ease-in-out;
  }

  .hamburger-line-top {
    transform-origin: 12px 6px;
  }

  .hamburger-line-middle {
    transform-origin: center;
  }

  .hamburger-line-bottom {
    transform-origin: 12px 18px;
  }

  .hamburger-icon.active .hamburger-line-middle {
    opacity: 0;
    transform: scaleX(0);
  }

  .hamburger-icon.active .hamburger-line-top {
    transform: translateY(6px) rotate(45deg);
  }

  .hamburger-icon.active .hamburger-line-bottom {
    transform: translateY(-6px) rotate(-45deg);
  }
</style>
